# disaster-models

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build disaster-models` to build the library.

## Running unit tests

Run `nx test disaster-models` to execute the unit tests via [Jest](https://jestjs.io).
